import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.stng.lkul',
  appName: 'lk-jur-site',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
