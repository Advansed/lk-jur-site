import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.stng.lkul',
  appName: 'СТНГ ЮЛ',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
